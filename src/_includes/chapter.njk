---
layout: 'base.njk'
---

{% include 'nav.njk' %}

{{ content | safe }}

<div class="grid contents">
    {% for item in contents %}
        {% if item.type == "sectiontitle" %}
            <p class="start1 end4 sectiontitle">{{ item.string }}</p>
        {% elif item.type == "largetext" %}
            <div class="largeText start4 end13">
                <h1>{{ item.string }}</h1>
            </div>
        {% elif item.type == "smalltext" %}
            <p class="start4 end10">{{ item.string }}</p>
        {% elif item.type == "largeimage" %}
            <div class="start4 end10 largeImage">
                {% image item.image, "Alt text", "(min-width: 600px) 50vw, 100vw" %}
            </div>
        {% endif %}
    {% endfor %}
</div>